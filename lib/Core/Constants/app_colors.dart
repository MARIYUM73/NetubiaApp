import 'package:flutter/material.dart';

class AppColors {
  // --- Background Colors (Dark Navy Gradient) ---
  // Uper wala dark hissa
  static const Color bgcolor = Color(0xFF021232); 
  // Neechy wala thora sa light hissa (Depth k liye)
  //static const Color bgDarkBottom = Color(0xFF000515); 

  // --- Primary/Button Colors (Cyan to Blue Gradient) ---
  // Button ka left side (Bright Cyan)
  static const Color primaryCyan = Color(0xFF0BB6E3); 
  // Button ka right side (Deep Blue)
  static const Color primaryBlue = Color(0xFF2561F2); 

  // --- Text Colors ---
static const Color whiteText = Color(0xFFFFFFFF);
  static const Color offWhiteText = Color(0xFFFCFCFC);
}